import { Member } from "../models/member";
import { User } from "../models/user";

export function createUser(user: User): AuthResponse {

}

export function getUser(user: User): User {

}

export function createMember(member: Member): void {
    
}

export function getMemberById(memberId: string): Member {
    
}